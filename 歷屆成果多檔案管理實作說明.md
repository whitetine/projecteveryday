# 歷屆成果多檔案管理實作說明

## 📁 檔案資料夾路徑規劃

### 路徑結構
```
uploads/
└── archive/
    └── {year}/          # 年份（從 cohortdata.year_label 提取，例如 "110"）
        └── {team_ID}/   # 團隊ID
            └── {fid}_{safeName}.{ext}  # 實際檔案
```

### 範例
- 110學年度，團隊ID為5的檔案：
  - `uploads/archive/110/5/1765421061_a3b4c5d6_專題報告.pdf`

### 路徑生成邏輯
1. 從 `teamdata.cohort_ID` 取得屆別
2. 從 `cohortdata.year_label` 提取年份（例如 "110學年度" → "110"）
3. 使用 `team_ID` 作為第二層目錄
4. 檔案名稱格式：`{fid}_{安全檔名}.{副檔名}`

---

## 📝 content_json 格式

### Schema
```json
{
  "intro": "專題簡介...",
  "files": [
    {
      "fid": "1765421061_a3b4c5d6",
      "name": "專題報告.pdf",
      "stored": "1765421061_a3b4c5d6_專題報告.pdf",
      "path": "uploads/archive/110/5/",
      "size": 2048576,
      "mime": "application/pdf",
      "uploaded_at": "2025-12-10 22:19:39",
      "allowDownload": false,
      "visible": true
    }
  ]
}
```

### 欄位說明
- `fid`: 唯一檔案ID（時間戳 + 亂數）
- `name`: 原始檔名（顯示用）
- `stored`: 實際存檔名（避免重名）
- `path`: 存放資料夾（相對路徑）
- `size`: 檔案大小（bytes）
- `mime`: MIME 類型
- `uploaded_at`: 上傳時間
- `allowDownload`: 是否允許下載（科辦控制）
- `visible`: 是否顯示（可選）

---

## 🔧 PHP 實作方式

### 1. 寫入 content_json

#### 上傳檔案時（`pages/archive_upload_api.php`）
```php
// 解析現有的 content_json
$contentJson = json_decode($submission['content_json'], true);
if (!$contentJson || !is_array($contentJson)) {
    $contentJson = [];
}

// 確保 files 陣列存在
if (!isset($contentJson['files']) || !is_array($contentJson['files'])) {
    $contentJson['files'] = [];
}

// 建立檔案資訊
$fileInfo = [
    'fid' => $fid,
    'name' => $fileName,
    'stored' => $storedName,
    'path' => 'uploads/archive/' . $year . '/' . $team_ID . '/',
    'size' => $fileSize,
    'mime' => $mimeType,
    'uploaded_at' => date('Y-m-d H:i:s'),
    'allowDownload' => false, // 預設不允許
    'visible' => true
];

// 添加到 files 陣列
$contentJson['files'][] = $fileInfo;

// 更新資料庫
$updateStmt = $conn->prepare("
    UPDATE prosubdata
    SET content_json = ?,
        prosub_update_d = NOW()
    WHERE prosub_ID = ?
");
$updateStmt->execute([
    json_encode($contentJson, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES),
    $prosub_ID
]);
```

#### 更新下載權限時（`pages/archive_toggle_download.php`）
```php
// 解析 content_json
$contentJson = json_decode($submission['content_json'], true);

// 查找並更新對應的檔案
foreach ($contentJson['files'] as &$file) {
    if (isset($file['fid']) && $file['fid'] === $fid) {
        $file['allowDownload'] = $allowDownload;
        break;
    }
}

// 更新資料庫
$updateStmt = $conn->prepare("
    UPDATE prosubdata
    SET content_json = ?,
        prosub_update_d = NOW()
    WHERE prosub_ID = ?
");
$updateStmt->execute([
    json_encode($contentJson, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES),
    $prosub_ID
]);
```

---

## 🔒 download.php 驗證下載流程

### 完整流程

```php
// 1. 驗證參數
$prosub_ID = (int)$_GET['prosub_ID'];
$fid = trim($_GET['fid']);

// 2. 查詢成果資料
$stmt = $conn->prepare("
    SELECT 
        ps.prosub_ID,
        ps.content_json,
        ps.prosub_status,
        pd.pro_status
    FROM prosubdata ps
    LEFT JOIN projectdata pd ON ps.pro_ID = pd.pro_ID
    WHERE ps.prosub_ID = ?
");
$stmt->execute([$prosub_ID]);
$submission = $stmt->fetch(PDO::FETCH_ASSOC);

// 3. 檢查成果狀態
if (!in_array($submission['prosub_status'], [1, 3])) {
    http_response_code(403);
    die('此成果尚未審核通過');
}

// 4. 檢查專題狀態
if ($submission['pro_status'] != 1) {
    http_response_code(403);
    die('專題已停用');
}

// 5. 解析 content_json 並查找檔案
$contentJson = json_decode($submission['content_json'], true);
$targetFile = null;
foreach ($contentJson['files'] as $file) {
    if ($file['fid'] === $fid) {
        $targetFile = $file;
        break;
    }
}

// 6. 檢查下載權限
if (!$targetFile['allowDownload']) {
    http_response_code(403);
    die('此檔案不允許下載');
}

// 7. 構建檔案路徑並驗證存在
$fullPath = __DIR__ . '/' . $targetFile['path'] . $targetFile['stored'];
if (!file_exists($fullPath)) {
    http_response_code(404);
    die('檔案不存在');
}

// 8. 設定 HTTP 標頭並輸出檔案
header('Content-Type: ' . $targetFile['mime']);
header('Content-Disposition: attachment; filename="' . addslashes($targetFile['name']) . '"');
header('Content-Length: ' . filesize($fullPath));
readfile($fullPath);
```

### 錯誤處理

| HTTP 狀態碼 | 情況 | 訊息 |
|------------|------|------|
| 400 | 參數錯誤 | "參數錯誤" |
| 403 | 無權限/未審核/不允許下載 | "此檔案不允許下載" |
| 404 | 成果/檔案不存在 | "檔案不存在" |
| 500 | 伺服器錯誤 | "伺服器錯誤" |

---

## 📋 已建立的檔案

### 1. 科辦管理頁面
- **檔案**: `pages/history_project_file.php`
- **功能**: 顯示所有成果的檔案清單，提供下載權限切換開關

### 2. 前端 JavaScript
- **檔案**: `js/history_project_file.js`
- **功能**: 處理列表載入、篩選、切換下載權限

### 3. 下載驗證
- **檔案**: `download.php`
- **功能**: 驗證下載權限並串流輸出檔案

### 4. 切換下載權限 API
- **檔案**: `pages/archive_toggle_download.php`
- **功能**: 更新 `content_json.files[].allowDownload`

### 5. 上傳多檔案 API
- **檔案**: `pages/archive_upload_api.php`
- **功能**: 處理多檔案上傳，更新 `content_json.files`

### 6. 成果列表 API
- **檔案**: `pages/history_api.php` (新增 `get_submissions_with_files`)
- **功能**: 取得成果列表（含檔案資訊）

---

## 🎯 使用方式

### 科辦上傳檔案
```javascript
const formData = new FormData();
formData.append('prosub_ID', prosub_ID);
formData.append('action', 'upload');
// 多檔案
for (let file of files) {
    formData.append('files[]', file);
}

fetch('pages/archive_upload_api.php', {
    method: 'POST',
    body: formData
});
```

### 前台下載檔案
```html
<a href="download.php?prosub_ID=123&fid=1765421061_a3b4c5d6">
    下載檔案
</a>
```

### 切換下載權限
```javascript
fetch('pages/archive_toggle_download.php', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
    },
    body: new URLSearchParams({
        prosub_ID: 123,
        fid: '1765421061_a3b4c5d6',
        allowDownload: '1'
    })
});
```

---

## ⚠️ 注意事項

1. **資料夾權限**: 確保 `uploads/archive/` 目錄有寫入權限（建議 775）
2. **檔案大小限制**: 目前設定為 100MB，可在 `archive_upload_api.php` 調整
3. **安全性**: 
   - 下載必須通過 `download.php`，不可直接訪問檔案 URL
   - 所有參數都經過驗證和轉義
4. **向後兼容**: 現有的 `content_json` 結構不會被破壞，只會新增 `files` 陣列

---

## 🔄 後續擴充建議

1. **前台瀏覽頁面**: 在 `project_browse.php` 中顯示檔案清單（僅顯示 `allowDownload=true` 的檔案）
2. **檔案刪除功能**: 允許科辦刪除已上傳的檔案
3. **批次操作**: 支援批次切換下載權限
4. **下載統計**: 記錄下載次數和時間

